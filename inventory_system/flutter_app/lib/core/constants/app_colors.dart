import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors (SaaS Modern Techy)
  static const primary = Color(0xFF6366F1); // Indigo Neon / Electric
  static const primaryLight = Color(0xFF818CF8);
  static const primaryDark = Color(0xFF4F46E5);

  static const secondary = Color(0xFF0EA5E9); // Bright electric cyan
  static const secondaryLight = Color(0xFF38BDF8);
  static const secondaryDark = Color(0xFF0284C7);

  // Accent Colors
  static const accent = Color(0xFFD946EF); // Fuchsia Neon
  static const accentLight = Color(0xFFF5D0FE);

  // Status & Utility Colors
  static const success = Color(0xFF10B981); // Emerald
  static const successLight = Color(0xFFD1FAE5);
  static const successDark = Color(0xFF065F46);

  static const warning = Color(0xFFF59E0B); // Amber
  static const warningLight = Color(0xFFFEF3C7);
  static const warningDark = Color(0xFF92400E);

  static const error = Color(0xFFEF4444); // Red rose
  static const errorLight = Color(0xFFFEE2E2);
  static const errorDark = Color(0xFF991B1B);

  // Techy Dark Theme Neutrals (Dark Slate Palette)
  static const neutral50 = Color(0xFFF8FAFC);
  static const neutral100 = Color(0xFFF1F5F9);
  static const neutral200 = Color(0xFFE2E8F0);
  static const neutral300 = Color(0xFFCBD5E1);
  static const neutral400 = Color(0xFF94A3B8);
  static const neutral500 = Color(0xFF64748B);
  static const neutral600 = Color(0xFF475569);
  static const neutral700 = Color(0xFF334155);
  static const neutral800 = Color(0xFF1E293B); // Deep slate
  static const neutral900 = Color(0xFF0F172A); // Tech background

  // Background and Surfaces (Dark Theme)
  static const background = Color(0xFF0B0F19); // Ultra dark space/slate
  static const surface = Color(0xFF161F30); // Techy card panels
  static const surfaceVariant = Color(0xFF1E293B); // Focused input/panel

  // Stock status
  static const stockLow = Color(0xFF451A20);
  static const stockWarning = Color(0xFF4A3415);
  static const stockGood = Color(0xFF133B27);
  static const stockLowText = Color(0xFFFCA5A5);
  static const stockWarningText = Color(0xFFFCD34D);
  static const stockGoodText = Color(0xFF6EE7B7);

  // Role colors
  static const roleAdmin = Color(0xFF8B5CF6); // Neon violet
  static const roleManager = Color(0xFF06B6D4); // Cyan
  static const roleStaff = Color(0xFF10B981); // Emerald
}
